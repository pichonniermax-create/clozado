/**
 * Le paramètre d'URL dédié aux erreurs de la section rendez-vous — la
 * fiche contact porte aussi les tâches (`erreur`) et le journal
 * (`erreurJournal`) : trois formulaires, trois messages, jamais confondus.
 */
export const APPOINTMENT_ERROR_PARAM = "erreurRendezVous";
